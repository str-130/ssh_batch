#脚本目前只能使用SSH,输入的内容有以下几个:
#IP:设备IP地址
#用户名:设备用户名
#密码:设备密码。由于是明文保存，需要注意安全性
#设备类型：关联输入的命令。脚本内搜索class Config，其中的dev_list中有设备类型与执行脚本的关系。
#	如果要使用自定义命令，将设备类型指定为none，使用config.txt文件编辑命令。一行一条命令。
#设备名，不写则为IP地址。生成文件时使用。
#端口:SSH的端口，不填写时为22
#是否需要进入使能模式：需要时在此列填入密码即可，不需要时可不填。
#当前为20线程，修改sem=threading.Semaphore(20)值可以改变最大线程数。
#在最前放加入#号可以忽略此行
#设备信息输入方式如下：
#10.1.1.100 username password cisco mytest001 22 admin
#10.1.1.100 username password
#必须按顺序填写，以空格分隔。使用 ' 作为占位符。如果使用自定义命令、端口为22、不需要进入使能模式，那么可以只输入IP、用户名、密码。
# IP	用户名	密码	设备类型	设备名	端口	是否需进入使能(enable)模式
